python --version # Python install
python -m venv venv # Virtual environment create
venv\Scripts\activate # Virtual env activate
pip install requests networkx boto3 pytest matplotlib # Install relvent libraries
// SOMETIME AN ERROR IS OCCUR LETS FIX IT
python -m pip install --upgrade pip # pip upgrade
pip install requests networkx boto3 pytest matplotlib python-dateutil # Correct way to install ALL required librarie
pip list # Confirm the installation


